# TrieSense-Smart-Sentence-Predictor
We will implement the Trie data structure in Java, supporting fast insertion and prefix lookup.  The system will allow dynamic word insertion and real-time suggestions.
